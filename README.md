####Referral Credit System
- docker compose up --build -d

- docker compose down


docker compose down --volumes
docker compose up --build -d
docker compose logs -f backend

Let me know what output you get after running the logs!